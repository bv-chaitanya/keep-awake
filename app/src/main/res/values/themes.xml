<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme with Material You dynamic color support -->
    <style name="Theme.KeepAwake" parent="Theme.Material3.DynamicColors.DayNight.NoActionBar">
        <!-- Prevent drawing behind display cutout -->
        <item name="android:windowLayoutInDisplayCutoutMode">never</item>
    </style>

    <!-- Transparent theme for the keep-awake Activity -->
    <style name="Theme.Transparent" parent="Theme.Material3.DynamicColors.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowLayoutInDisplayCutoutMode">never</item>
    </style>

</resources>
